<%= turbo_stream.remove dom_id(@product) %> <!-- Removes the product from the list -->

<%= turbo_stream.replace 'main' do %>
  <%= render 'products/products_index', products: Product.page(params[:page]).per(5) %> <!-- Renders the updated products index -->
<% end %>