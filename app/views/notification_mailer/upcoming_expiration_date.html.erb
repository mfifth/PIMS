<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f9fafb;
      color: #333;
    }

    .container {
      width: 100%;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #ffffff;
      border: 1px solid #ddd;
      border-radius: 8px;
    }

    h1 {
      font-size: 24px;
      color: #2c3e50;
      text-align: center;
      margin-bottom: 20px;
    }

    p {
      font-size: 16px;
      line-height: 1.6;
      color: #555;
    }

    .highlight {
      font-weight: bold;
      color: #e74c3c;
    }

    .batch-number {
      color: #2980b9;
      font-weight: bold;
    }

    .footer {
      margin-top: 20px;
      text-align: center;
      font-size: 14px;
      color: #777;
    }

    .footer a {
      color: #3498db;
      text-decoration: none;
    }

    .button {
      display: inline-block;
      padding: 12px 20px;
      background-color: #2980b9;
      color: #fff;
      font-size: 16px;
      text-align: center;
      border-radius: 5px;
      text-decoration: none;
      margin-top: 20px;
    }

    .button:hover {
      background-color: #3498db;
    }

    .product-table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
    }

    .product-table th {
      background-color: #f2f2f2;
      text-align: left;
      padding: 10px;
      border-bottom: 2px solid #ddd;
    }

    .product-table td {
      padding: 10px;
      border-bottom: 1px solid #eee;
    }

    .product-table tr:last-child td {
      border-bottom: none;
    }

    .location-badge {
      display: inline-block;
      padding: 3px 8px;
      background-color: #e3f2fd;
      border-radius: 4px;
      font-size: 14px;
      color: #1976d2;
      margin-right: 5px;
      margin-bottom: 5px;
    }

    .quantity-badge {
      display: inline-block;
      padding: 3px 8px;
      background-color: #e8f5e9;
      border-radius: 4px;
      font-size: 14px;
      color: #2e7d32;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Upcoming Expiration Date Alert</h1>

    <p>Dear <%= @user.name %>,</p>

    <p>We wanted to notify you that the batch <span class="batch-number"><%= @batch.batch_number %></span> is approaching its expiration date.</p>

    <p><span class="highlight">Expiration Date:</span> <%= @batch.expiration_date.strftime('%B %d, %Y') %></p>

    <h3>Products in this batch:</h3>

    <table class="product-table">
      <thead>
        <tr>
          <th>Product</th>
          <th>Locations & Quantities</th>
        </tr>
      </thead>
      <tbody>
        <% @batch.products.each do |product| %>
          <tr>
            <td>
              <strong><%= product.name %></strong><br>
              <small>SKU: <%= product.sku %></small>
            </td>
            <td>
              <% product.inventory_items.group_by(&:location).each do |location, items| %>
                <div>
                  <span class="location-badge"><%= location.name %></span>
                  <span class="quantity-badge">
                    <%= items.sum(&:quantity) %> units
                  </span>
                </div>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <p>If you need to take any action, please review the batch details in your account.</p>

    <a href="<%= url_for(controller: 'batches', action: 'show', id: @batch.id) %>" class="button">View Batch Details</a>

    <div class="footer">
      <p>Best regards,</p>
      <p>Pimsco</p>
      <p><a href="http://pimsco.tech">www.pimsco.tech</a></p>
    </div>
  </div>

</body>
</html>