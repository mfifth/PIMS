<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    /* [Previous CSS remains exactly the same] */
  </style>
</head>
<body>
  <div class="container">
    <h1><%= t('mailers.expiration_alert.title') %></h1>
    <p><%= t('mailers.greeting', name: @user.name) %>,</p>
    <p><%= t('mailers.expiration_alert.notification', batch_number: content_tag(:span, @batch.batch_number, class: 'batch-number')).html_safe %></p>
    <p><span class="highlight"><%= t('mailers.expiration_alert.expiration_date') %>:</span> <%= @batch.expiration_date.strftime(t('date.formats.default')) %></p>
    
    <h3><%= t('mailers.expiration_alert.products_in_batch') %></h3>
    
    <table class="product-table">
      <thead>
        <tr>
          <th><%= t('mailers.expiration_alert.product') %></th>
          <th><%= t('mailers.expiration_alert.locations_quantities') %></th>
        </tr>
      </thead>
      <tbody>
        <% @batch.products.each do |product| %>
          <tr>
            <td>
              <strong><%= product.name %></strong><br>
              <small><%= t('mailers.expiration_alert.sku') %>: <%= product.sku %></small>
            </td>
            <td>
              <% product.inventory_items.group_by(&:location).each do |location, items| %>
                <div>
                  <span class="location-badge"><%= location.name %></span>
                  <span class="quantity-badge">
                    <%= items.sum(&:quantity) %> <%= t('mailers.expiration_alert.units') %>
                  </span>
                </div>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <p><%= t('mailers.expiration_alert.follow_up') %></p>
    <a href="<%= url_for(controller: 'batches', action: 'show', id: @batch.id) %>" class="button"><%= t('mailers.expiration_alert.view_details') %></a>
    
    <div class="footer">
      <%= t('mailers.signature', company_name: "Pimsco") %>
      <p><a href="http://pimsco.tech">www.pimsco.tech</a></p>
    </div>
  </div>
</body>
</html>