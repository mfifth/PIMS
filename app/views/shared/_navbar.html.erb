<!-- Navbar -->
<div class="py-3 px-6 bg-[#f8f4f3] flex items-center justify-between shadow-md shadow-black/5 sticky top-0 left-0 z-30">
  <!-- Mobile Hamburger -->
  <button data-action="click->sidebar#toggle" class="md:hidden">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2"
        viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
        <path d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>

  <h1 class="text-xl font-semibold text-gray-800">Welcome <%= Current.user.name %></h1>
</div>
