{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/*.{js,ts,css} --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets"
  },
  "devDependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.13",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.2",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.3"
  },
  "dependencies": {
    "@hotwired/turbo": "^8.0.13",
    "stimulus-use": "^0.52.3"
  }
}
