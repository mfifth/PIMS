{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/*.{js,ts} --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets"
  },
  "devDependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.13",
    "esbuild": "^0.14.0"
  },
  "dependencies": {
    "@hotwired/turbo": "^8.0.13",
    "stimulus-use": "^0.52.3"
  }
}
